So let's check out the new loop or iteration feature in Cable Test Automation 25.12 or higher.  So here I'm on a Cable 25.12 environment brand new.  And in this release and later of course Oracle has added two new keywords.  So we are on the release screen and let's check out the keywords.  And let's just do account.  So here are 70. So there are two more than before.  And let's find them.  Should something with loop.  Here they are. So there's the start loop.  And has a help text of course defines the start of the loop.  Optionally specify a variable to store the current iteration number on each run.  Very important. And then the end loop defines the end of the loop.